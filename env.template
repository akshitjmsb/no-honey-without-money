# Copy this file to .env.local and fill in your actual values
# Never commit .env.local to version control

# Backend API URL (for production, change this to your deployed backend URL)
VITE_API_URL=http://localhost:3001

# Note: The Google Gemini API key is now stored securely on the backend server
# You no longer need to expose it in the frontend environment variables
